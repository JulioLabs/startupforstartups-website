baseURL = 'https://presencewave.com'
languageCode = 'en-us'
title = 'PresenceWave - Digital Presence Platform for Businesses'
theme = 'startup-theme'

# Enable unsafe HTML in markdown (needed for custom HTML in content)
[markup]
  [markup.goldmark]
    [markup.goldmark.renderer]
      unsafe = true
      hardWraps = false
    [markup.goldmark.parser]
      autoHeadingID = true
      autoHeadingIDType = "github"

# Configure menus for navigation
[menu]
  [[menu.main]]
    name = "Features"
    url = "/#features"
    weight = 10

  [[menu.main]]
    name = "Platform"
    url = "/#platform"
    weight = 20

  [[menu.main]]
    name = "Get Started"
    url = "https://app.startupforstartups.com"
    weight = 30

# Site parameters
[params]
  description = "The complete digital presence solution for businesses. Job boards, blogs, widgets, analytics and more - all the tools you need in one unified platform."

  # Footer configuration
  [params.footer]
    copyright = "Julio Labs (OPC) Pvt. Ltd. All rights reserved."

  # Social links
  [params.social]
    twitter = "https://twitter.com/get_s4s"
    blog = "https://blog.presencewave.com"

# Output formats for different content types
[outputs]
  home = ["HTML", "RSS"]
  page = ["HTML"]

# SEO and meta configuration
[params.seo]
  title = "PresenceWave - Digital Presence Platform for Businesses"
  description = "The complete digital presence solution for businesses. Job boards, blogs, widgets, analytics and more - all the tools you need in one unified platform."
  image = "/assets/images/heroimage.png"
